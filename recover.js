const fs = require('fs'); const lines = fs.readFileSync('C:/Users/Rangel/.gemini/antigravity-ide/brain/b058ec60-d889-4d6a-a0c1-2e5d61d35026/.system_generated/logs/transcript.jsonl', 'utf8').split('\n'); for(let line of lines) { if(line.includes('UploadExcelModal.jsx') && line.includes('write_to_file')) { let obj = JSON.parse(line); let tc = obj.tool_calls.find(t => t.name === 'write_to_file' || t.tool_name === 'default_api:write_to_file'); if(tc) { let args = tc.args || tc.arguments; fs.writeFileSync('R:/Meu Drive/Painel Maxxcontrol-x-sistema/web/src/components/UploadExcelModal.jsx', args.CodeContent); console.log('Recovered!'); break; } } }
